export class Cart{


    itemKey: number;
    itemName: string;
    quantity: number;
    totalPrice: number;

    constructor(itemKey:number,itemName:string,quantity: number,totalPrice: number){
        this.itemKey = itemKey;
        this.itemName = itemName;
        this.quantity = quantity;
        this.totalPrice = totalPrice;
    }
}